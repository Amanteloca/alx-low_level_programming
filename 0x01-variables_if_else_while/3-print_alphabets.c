#include <stdio.h>

int main ()

{

        char pha;

        for (pha = 'a', pha <= 'z' , pha++)
        
	putchar(pha);

	for (pha = 'A', pha <= 'Z' , pha++)

        putchar(pha);
 


        putchar(\n);
        return 0;
}
